//! Slash-command helper modules used by the interactive CLI.

/// `login`/`logout` provider resolution helpers.
pub(crate) mod auth;
/// `/model` command helpers.
pub(crate) mod model;
/// `/session` command helpers.
pub(crate) mod session;
/// `/theme` command helpers.
pub(crate) mod theme;
