# Model context window catalog used by src/tokens.rs.
#
# Snapshot source:
#   - https://openrouter.ai/api/v1/models (contains `id` + `context_length`)
# Fetched on:
#   - 2026-02-18
#
# Rules are evaluated top-to-bottom. Put specific entries first.

schema_version = 1
source = "openrouter-models-api"
source_url = "https://openrouter.ai/api/v1/models"
retrieved_at = "2026-02-18"
default_context_window = 8192

# ---------------------------------------------------------------------------
# OpenAI
# ---------------------------------------------------------------------------

[[rule]]
match = "exact"
pattern = "gpt-5-chat"
context_window = 128000

[[rule]]
match = "prefix"
pattern = "gpt-5"
context_window = 400000

[[rule]]
match = "prefix"
pattern = "gpt-4.1"
context_window = 1047576

[[rule]]
match = "prefix"
pattern = "gpt-4o"
context_window = 128000

[[rule]]
match = "prefix"
pattern = "gpt-4-turbo"
context_window = 128000

[[rule]]
match = "exact"
pattern = "gpt-4"
context_window = 8191

[[rule]]
match = "exact"
pattern = "gpt-3.5-turbo-0613"
context_window = 4095

[[rule]]
match = "exact"
pattern = "gpt-3.5-turbo-instruct"
context_window = 4095

[[rule]]
match = "prefix"
pattern = "gpt-3.5-turbo-16k"
context_window = 16385

[[rule]]
match = "prefix"
pattern = "gpt-3.5-turbo"
context_window = 16385

[[rule]]
match = "exact"
pattern = "o1"
context_window = 200000

[[rule]]
match = "exact"
pattern = "o1-pro"
context_window = 200000

[[rule]]
match = "exact"
pattern = "o3"
context_window = 200000

[[rule]]
match = "exact"
pattern = "o3-mini"
context_window = 200000

[[rule]]
match = "exact"
pattern = "o3-pro"
context_window = 200000

[[rule]]
match = "exact"
pattern = "o4-mini"
context_window = 200000

[[rule]]
match = "prefix"
pattern = "o1-"
context_window = 200000

[[rule]]
match = "prefix"
pattern = "o3-"
context_window = 200000

[[rule]]
match = "prefix"
pattern = "o4-mini"
context_window = 200000

# ---------------------------------------------------------------------------
# Anthropic Claude
# ---------------------------------------------------------------------------

[[rule]]
match = "exact"
pattern = "claude-opus-4.6"
context_window = 1000000

[[rule]]
match = "prefix"
pattern = "claude-sonnet-4"
context_window = 1000000

[[rule]]
match = "prefix"
pattern = "claude-opus-4"
context_window = 200000

[[rule]]
match = "prefix"
pattern = "claude-3.7-sonnet"
context_window = 200000

[[rule]]
match = "prefix"
pattern = "claude-3.5-sonnet"
context_window = 200000

[[rule]]
match = "prefix"
pattern = "claude-3.5-haiku"
context_window = 200000

[[rule]]
match = "prefix"
pattern = "claude-3-haiku"
context_window = 200000

# ---------------------------------------------------------------------------
# Google Gemini
# ---------------------------------------------------------------------------

[[rule]]
match = "prefix"
pattern = "gemini-3-pro"
context_window = 1048576

[[rule]]
match = "prefix"
pattern = "gemini-3-flash"
context_window = 1048576

[[rule]]
match = "prefix"
pattern = "gemini-2.5-pro"
context_window = 1048576

[[rule]]
match = "prefix"
pattern = "gemini-2.5-flash-lite"
context_window = 1048576

[[rule]]
match = "prefix"
pattern = "gemini-2.5-flash"
context_window = 1048576

[[rule]]
match = "prefix"
pattern = "gemini-2.0-flash"
context_window = 1048576

# ---------------------------------------------------------------------------
# Moonshot Kimi
# ---------------------------------------------------------------------------

[[rule]]
match = "prefix"
pattern = "kimi-k2.5"
context_window = 262144

[[rule]]
match = "prefix"
pattern = "kimi-k2-thinking"
context_window = 262144

[[rule]]
match = "prefix"
pattern = "kimi-k2"
context_window = 131072

# ---------------------------------------------------------------------------
# DeepSeek
# ---------------------------------------------------------------------------

[[rule]]
match = "prefix"
pattern = "deepseek-v3.2"
context_window = 163840

[[rule]]
match = "prefix"
pattern = "deepseek-chat-v3.1"
context_window = 32768

[[rule]]
match = "prefix"
pattern = "deepseek-chat"
context_window = 163840

[[rule]]
match = "prefix"
pattern = "deepseek-r1"
context_window = 64000

# ---------------------------------------------------------------------------
# Mistral
# ---------------------------------------------------------------------------

[[rule]]
match = "prefix"
pattern = "codestral-2508"
context_window = 256000

[[rule]]
match = "prefix"
pattern = "devstral-2512"
context_window = 262144

[[rule]]
match = "prefix"
pattern = "devstral-medium"
context_window = 131072

[[rule]]
match = "prefix"
pattern = "devstral-small"
context_window = 131072

[[rule]]
match = "prefix"
pattern = "mistral-large-2512"
context_window = 262144

[[rule]]
match = "prefix"
pattern = "mistral-large-2411"
context_window = 131072

[[rule]]
match = "prefix"
pattern = "mistral-large-2407"
context_window = 131072

[[rule]]
match = "prefix"
pattern = "mistral-large"
context_window = 128000

[[rule]]
match = "prefix"
pattern = "mistral-medium-3.1"
context_window = 131072

[[rule]]
match = "prefix"
pattern = "mistral-medium-3"
context_window = 131072

[[rule]]
match = "prefix"
pattern = "mistral-small-3.2"
context_window = 131072

[[rule]]
match = "prefix"
pattern = "mistral-small-3.1"
context_window = 128000

[[rule]]
match = "prefix"
pattern = "mistral-7b-instruct-v0.2"
context_window = 32768

[[rule]]
match = "prefix"
pattern = "mixtral-8x7b-instruct"
context_window = 32768

# ---------------------------------------------------------------------------
# Llama
# ---------------------------------------------------------------------------

[[rule]]
match = "prefix"
pattern = "llama-4-maverick"
context_window = 1048576

[[rule]]
match = "prefix"
pattern = "llama-4-scout"
context_window = 327680

[[rule]]
match = "prefix"
pattern = "llama-3.3-70b-instruct"
context_window = 131072

[[rule]]
match = "prefix"
pattern = "llama-3.2-11b-vision-instruct"
context_window = 131072

[[rule]]
match = "prefix"
pattern = "llama-3.2-3b-instruct"
context_window = 131072

[[rule]]
match = "prefix"
pattern = "llama-3.2-1b-instruct"
context_window = 60000

[[rule]]
match = "prefix"
pattern = "llama-3.1-405b-instruct"
context_window = 131000

[[rule]]
match = "prefix"
pattern = "llama-3.1-70b-instruct"
context_window = 131072

[[rule]]
match = "prefix"
pattern = "llama-3.1-8b-instruct"
context_window = 16384

[[rule]]
match = "prefix"
pattern = "llama-3-70b-instruct"
context_window = 8192

[[rule]]
match = "prefix"
pattern = "llama-3-8b-instruct"
context_window = 8192

# ---------------------------------------------------------------------------
# Qwen
# ---------------------------------------------------------------------------

[[rule]]
match = "prefix"
pattern = "qwen3.5-plus"
context_window = 1000000

[[rule]]
match = "prefix"
pattern = "qwen3.5-397b-a17b"
context_window = 262144

[[rule]]
match = "prefix"
pattern = "qwen3-coder-plus"
context_window = 1000000

[[rule]]
match = "prefix"
pattern = "qwen3-coder-flash"
context_window = 1000000

[[rule]]
match = "prefix"
pattern = "qwen3-coder-next"
context_window = 262144

[[rule]]
match = "prefix"
pattern = "qwen3-coder"
context_window = 262144

[[rule]]
match = "prefix"
pattern = "qwen3-235b-a22b-2507"
context_window = 262144

[[rule]]
match = "prefix"
pattern = "qwen3-235b-a22b"
context_window = 131072

[[rule]]
match = "prefix"
pattern = "qwen3-max"
context_window = 262144

[[rule]]
match = "prefix"
pattern = "qwen-plus"
context_window = 1000000

[[rule]]
match = "prefix"
pattern = "qwen-max"
context_window = 32768

[[rule]]
match = "prefix"
pattern = "qwen2.5-coder-7b-instruct"
context_window = 32768

[[rule]]
match = "prefix"
pattern = "qwen-2.5-72b-instruct"
context_window = 32768

[[rule]]
match = "prefix"
pattern = "qwen-2.5-7b-instruct"
context_window = 32768

# ---------------------------------------------------------------------------
# Family fallback rules
# ---------------------------------------------------------------------------

[[rule]]
match = "contains"
pattern = "claude"
context_window = 200000

[[rule]]
match = "contains"
pattern = "gemini"
context_window = 1048576

[[rule]]
match = "contains"
pattern = "kimi"
context_window = 131072

[[rule]]
match = "contains"
pattern = "deepseek"
context_window = 64000

[[rule]]
match = "contains"
pattern = "qwen"
context_window = 32768

[[rule]]
match = "contains"
pattern = "mistral"
context_window = 32768

[[rule]]
match = "contains"
pattern = "llama"
context_window = 8192

[[rule]]
match = "contains"
pattern = "gpt-4o"
context_window = 128000

[[rule]]
match = "contains"
pattern = "gpt-4.1"
context_window = 1047576

[[rule]]
match = "contains"
pattern = "gpt-5"
context_window = 400000
